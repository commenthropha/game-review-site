import React from 'react'
import styles from "./styles/Hero.module.css"

const Hero = () => {
  return (
    <div id = "hero" className={`${styles.main}`}>
        Hero
    </div>
  )
}

export default Hero